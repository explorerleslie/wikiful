<h1><%= @category.name %></h1>

<h2>testing creating an instance variable in the show action for articles in category</h2>
<% @articles_in_category.each do |article| %>
<h3><%= link_to article.title, article %></h3>
<p>Publication Date: <%= article.created_at.strftime('%b %d, %Y') %>
<p><%= truncate(article.content, length: 200) %></p>
<% end %>

<h2>testing getting to articles from the @category object in the view template</h2>
<% @category.articles.each do |article| %>
<p><%= article.title %></p><% end %>